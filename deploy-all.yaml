# -----------------------------------------------------------------------
# 1. GATEWAY LAYER: FLASK COMPONENT + INTERNAL PARALLEL ENGINE (1 REPLICA)
# -----------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fyp-dashboard-deployment
spec:
  replicas: 1  # 1 Replica ensures an unadulterated, clean architectural benchmark environment
  selector:
    matchLabels:
      app: fyp-dashboard
  template:
    metadata:
      labels:
        app: fyp-dashboard
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - bw-worker
                - bright-worker
                - blur-worker
            topologyKey: "kubernetes.io/hostname"  # Prevents sharing a node with any distributed worker
      containers:
      - name: gateway-container
        image: asia-southeast1-docker.pkg.dev/YOUR_GCP_PROJECT_ID/fyp-images/dashboard-gateway:latest
        ports:
        - containerPort: 5000
        resources:
          requests:
            cpu: "3"         # GUARANTEES GKE maps 3 physical CPU cores to this pod for 3-worker Parallel execution
            memory: "3Gi"    # Prevents Out-Of-Memory crashing during heavy multi-process image mapping
          limits:
            cpu: "4"
            memory: "4Gi"
        livenessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 10
        env:
        - name: BW_ADDR
          value: "bw-dns-service:50051"
        - name: BRIGHT_ADDR
          value: "bright-dns-service:50052"
        - name: BLUR_ADDR
          value: "blur-dns-service:50053"
---
apiVersion: v1
kind: Service
metadata:
  name: fyp-dashboard-service
spec:
  type: LoadBalancer       
  selector:
    app: fyp-dashboard
  ports:
  - port: 80
    targetPort: 5000

---
# -----------------------------------------------------------------------
# 2. MICROSERVICE TIER: BLACK & WHITE WORKER POD (1 REPLICA)
# -----------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bw-worker-deployment
spec:
  replicas: 1  # Optimized from 3 replicas down to 1
  selector:
    matchLabels:
      app: bw-worker
  template:
    metadata:
      labels:
        app: bw-worker
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - fyp-dashboard
                - bright-worker
                - blur-worker
            topologyKey: "kubernetes.io/hostname"  # Hard segregation from all other cluster applications
      containers:
      - name: bw-container
        image: asia-southeast1-docker.pkg.dev/YOUR_GCP_PROJECT_ID/fyp-images/bw-worker:latest
        ports:
        - containerPort: 50051
        livenessProbe:
          tcpSocket:
            port: 50051
          initialDelaySeconds: 10
          periodSeconds: 5
        readinessProbe:
          tcpSocket:
            port: 50051
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: bw-dns-service
spec:
  type: ClusterIP  
  selector:
    app: bw-worker
  ports:
  - port: 50051
    targetPort: 50051

---
# -----------------------------------------------------------------------
# 3. MICROSERVICE TIER: BRIGHTNESS BOOST WORKER POD (1 REPLICA)
# -----------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bright-worker-deployment
spec:
  replicas: 1  # Optimized from 3 replicas down to 1
  selector:
    matchLabels:
      app: bright-worker
  template:
    metadata:
      labels:
        app: bright-worker
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - fyp-dashboard
                - bw-worker
                - blur-worker
            topologyKey: "kubernetes.io/hostname"  # Enforces a unique hardware machine for Brightness
      containers:
      - name: bright-container
        image: asia-southeast1-docker.pkg.dev/YOUR_GCP_PROJECT_ID/fyp-images/bright-worker:latest
        ports:
        - containerPort: 50052
        livenessProbe:
          tcpSocket:
            port: 50052
          initialDelaySeconds: 10
          periodSeconds: 5
        readinessProbe:
          tcpSocket:
            port: 50052
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: bright-dns-service
spec:
  type: ClusterIP
  selector:
    app: bright-worker
  ports:
  - port: 50052
    targetPort: 50052

---
# -----------------------------------------------------------------------
# 4. MICROSERVICE TIER: GAUSSIAN BLUR WORKER POD (1 REPLICA)
# -----------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blur-worker-deployment
spec:
  replicas: 1  # Optimized from 3 replicas down to 1
  selector:
    matchLabels:
      app: blur-worker
  template:
    metadata:
      labels:
        app: blur-worker
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - fyp-dashboard
                - bw-worker
                - bright-worker
            topologyKey: "kubernetes.io/hostname"  # Enforces a unique hardware machine for Blurring
      containers:
      - name: blur-container
        image: asia-southeast1-docker.pkg.dev/YOUR_GCP_PROJECT_ID/fyp-images/blur-worker:latest
        ports:
        - containerPort: 50053
        livenessProbe:
          tcpSocket:
            port: 50053
          initialDelaySeconds: 10
          periodSeconds: 5
        readinessProbe:
          tcpSocket:
            port: 50053
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: blur-dns-service
spec:
  type: ClusterIP
  selector:
    app: blur-worker
  ports:
  - port: 50053
    targetPort: 50053

---
# -----------------------------------------------------------------------
# 5. TELEMETRY VISUALIZATION TIER: GRAFANA INFRASTRUCTURE (1 REPLICA)
# -----------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fyp-grafana-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fyp-grafana
  template:
    metadata:
      labels:
        app: fyp-grafana
    spec:
      containers:
      - name: grafana-container
        image: grafana/grafana:latest
        ports:
        - containerPort: 3000
        env:
        - name: GF_SECURITY_ALLOW_EMBEDDING
          value: "true"
        - name: GF_SECURITY_COOKIE_SECURE
          value: "true"
        - name: GF_SECURITY_COOKIE_SAME_SITE
          value: "none"
        - name: GF_AUTH_ANONYMOUS_ENABLED
          value: "true"
        - name: GF_AUTH_ANONYMOUS_ORG_ROLE
          value: "Viewer"
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: grafana-dns-service
spec:
  type: LoadBalancer
  selector:
    app: fyp-grafana
  ports:
  - name: http           
    port: 80
    targetPort: 3000
